#NODE HTTP SERVER (WITHOUT EXPRESS JS)
###A HTTP server written in pure Node JS without the Express.js framework.
###Very few libraries were used in this project.
###The code is mostly Pure JavaScript.

```console
$ npm install
```
 
